#!/usr/bin/env ruby

#https://github.com/alloy/terminal-notifier 脚本地址

def checkWithCommand command
  result = `#{command}`
  `terminal-notifier -title "#{command}" -message "#{result}"` if result.length > 0
end

#Homebrew
`brew update`
`brew cleanup`
checkWithCommand "brew outdated"

#RubyGems
checkWithCommand "gem outdated"
`gem cleanup`
